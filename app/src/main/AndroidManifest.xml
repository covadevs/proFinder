<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="profinder.com.br.profinderproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icone"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/label_telalogin" />
        <activity
            android:name=".CadastroActivity"
            android:label="@string/label_telacadastro" />
        <activity android:name=".AlunoActivity" />
        <activity
            android:name=".ProfessorActivity"
            android:label="@string/title_activity_professor"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CadastroProjetoActivity"
            android:label="Projeto" />
        <activity android:name=".EditarProjetoActivity"></activity>
    </application>

</manifest>